# React Webpack TypeScript on Full Scratch

## Step 1: First Setting

### Create File

- Create Folder
  - build/
  - src/
- Create File
  - .gitignore
    - ignore `build` `node_modules`
- Create `package.json`
  - `npm init --y`
- Create `index.html` in src/